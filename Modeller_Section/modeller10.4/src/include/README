The files in this directory are C header (.h) files, which define the
interface to the Modeller library. They can be used directly in your own
C programs (see examples/commands/c-example.c) or to build the SWIG Python
interface (see the ../swig/ directory).
